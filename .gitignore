node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
*.log

# CI workflow parked here until the gh token has `workflow` scope.
# See DEPLOY.md.
.ci-pending/

# Capacitor native shells — generated on the build machine via `npx cap add`.
/ios/
/android/
